#!/usr/local/plan9/bin/rc
# Acme startup script

PLAN9=/usr/local/plan9
SHELL=$PLAN9/bin/rc

acmeshell=$SHELL
font=$PLAN9/font/lucsans/typeunicode.7.font
path=($path $PLAN9/bin)

pgrep plumber > /dev/null || plumber &
acme -c 1 -a -f $font $* -l acme.dump
